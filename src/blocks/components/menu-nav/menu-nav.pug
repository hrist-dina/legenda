

mixin menu-nav(data)
    nav.menu-nav.swiper-container.js-tabs-scroll&attributes(attributes)
        ul.__list.swiper-wrapper
            for item in data
                li.__item.swiper-slide
                    a(href='#' class={active: item.active}).__link
                        span=item.name