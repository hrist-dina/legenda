export default class ComponentBase {
    constructor(selector) {
        this.selector = selector

        this.init()
    }

    static getSelector() {
        // Need release
    }

    init() {
        // Need release
    }
}
