export default class ComponentBase {
    constructor(selector = null) {
        this.selector = selector

        this.init()
    }

    static getSelector() {
        // Need release
    }

    init() {
        // Need release
    }
}
